package main

import (
	// "context"
	// "os"
	// vault_infrastructure_crypto "vault-app/internal/vault/infrastructure/crypto"
)

func main() {
	// 1. Recipient reads:
	//    - EncryptedKey (from share)
	//    - CID (from attachment)
	// 2. Derive symKey from EncryptedKey

	//


    // config, err := 
	// aesService := vault_infrastructure_crypto.AESService{}

	// symKey, err := aesService.Decrypt(recipientPrivateKey, EncryptedKey)
	// if err != nil { /* panic */
	// }

	// // 3. Get raw bytes from IPFS
	// rawBytes, err := cloudClient.GetDataFromCloudStorage(context.Background(), req)
	// if err != nil { /* panic */
	// }

	// // 4. Decrypt with symKey
	// plain, err := crypto.DecryptSymKey(rawBytes, symKey)
	// if err != nil { /* panic */
	// }

	// // 5. Write to disk
	// err = os.WriteFile("attachment.jpg", plain, 0600)
	// if err != nil { /* panic */
	// }
}
